import com.example.shapes.Circle;
import com.example.shapes.Shapes;
import com.example.shapes.Square;
import com.example.tools.Hammer;
import com.example.tools.Tools;
import com.example.tools.Wrench;
import com.example.math.MathOperations;
import com.example.math.MathProperties;

public class Main {
    public static void main(String[] args) {

        //get the first argument passed in 
        System.out.println(args[0]);
        //get the second argument passed in
        System.out.println(args[1]);
        //create instance variable called args that is a int datatype
        //iterate over the args
        //set the instance variable to the args
        String firstArg = args[0];
        String secondArg = args[1];

        int firstInt = Integer.parseInt(firstArg);
        int secondInt = Integer.parseInt(secondArg);

        System.out.println(firstInt + secondInt);


        // why do i do the way i do?
        //what is the what to why i think the most simple shit has to be so complicated?

        //dont over analyiz and take it slow. 

        //have to have experience to know what road to go down otherwise its all exploratory

        //more fighting the less probably solution it is
        //if you findyourself doing 5 or 10 things its probably really 2
        //dont keep hacking and take a step back and see if what i am doing is even the right thing
        
        















        // Shapes circle = new Circle();
        // Shapes square = new Square();

        // Tools hammer = new Hammer();
        // Tools wrench = new Wrench();

        // MathProperties math = new MathOperations();

        // System.out.println(math.getSquare(6));
        // System.out.println(math.getSquareRoot(6));
        // System.out.println(math.largestInteger(6, 7));
        // int[] numbers = new int[]{3,2,5,4,1};
        // System.out.println(math.add(numbers));

        // System.out.println(circle.getShape());
        // System.out.println(circle.getColor());
        // System.out.println(square.getShape());
        // System.out.println(square.getColor());       

        // System.out.println(hammer.getTool());
        // System.out.println(hammer.getSize());
        // System.out.println(wrench.getTool());
        // System.out.println(wrench.getSize());
    }
}
//initalized array [numberheredeafult value] why is the default array size 0 and is that actually 0 or unlimited?